#include "../include/resource_handler.h"

size_t
service_request(struct request *req, char *rtrv_buf, const size_t len)
{
    size_t      nbytes = 0;
    // check method
    // access the cache
    return nbytes;
}
