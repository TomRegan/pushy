# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant::Config.run do |config|
    config.vm.define :cent do |cent_config|
        cent_config.vm.box = "cent-6.3-64"
    end
    config.vm.define :precise do |precise_config|
        precise_config.vm.box = "precise64"
    end
end
